OPENAI_API_KEY=
# Enforcement is OFF by default (0 = monitor only). Set a positive daily UTC token
# cap to make MeteredOpenAI raise DailyTokenCapReached before a call would exceed it.
OTM_DAILY_TOKEN_CAP=0
# Optional: override the machine-global ledger location.
# OTM_LEDGER_PATH=
